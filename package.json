{
  "name": "late-day-buying-method-watchlist",
  "version": "0.1.0",
  "private": true,
  "description": "Static mobile UI prototype for A-share late-day buying method watchlist.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp ui-prototype/*.html ui-prototype/*.css ui-prototype/*.js dist/",
    "dev": "node scripts/dev-server.js",
    "preview": "node scripts/dev-server.js"
  }
}
