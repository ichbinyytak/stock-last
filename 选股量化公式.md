# 尾盘买入法选股量化公式

记录版本：selection strategy v4

本文记录当前网站实际使用的选股量化规则。它是学习和模拟盘规则，不是投资建议。

## 1. 时间窗口

```text
如果 14:30 <= 当前时间 < 14:57：
  市场状态 = 尾盘窗口
  允许把合格股票标记为“尾盘候选”

如果 14:57 <= 当前时间 <= 15:00：
  市场状态 = 尾盘竞价
  只复盘，不追新仓

其他时间：
  只观察或复盘，不确认新买点
```

## 2. 板块原始评分

对每个候选板块，先取板块成分股，再计算原始分：

```text
板块原始分 =
  max(板块涨幅, 0) * 5
  + max(板块换手率, 0) * 0.35
  + 板块强弱差 * 20
  + 20cm/30cm前排数量 * 8
  + 10cm助攻数量 * 5
  + 活跃股数量 * 0.7
  + 尾盘活跃候选数量 * 2.5
```

其中：

```text
板块强弱差 = (上涨家数 - 下跌家数) / max(上涨家数 + 下跌家数, 1)

20cm/30cm前排数量 =
  板块内创业板、科创板、北交所等20cm/30cm股票中，涨幅 >= 15% 的数量

10cm助攻数量 =
  板块内主板10cm股票中，涨幅 >= 9% 的数量

活跃股数量 =
  板块内涨幅 >= 5% 的股票数量

尾盘活跃候选数量 =
  板块内涨幅 >= 5% 且涨幅 <= 18% 且换手率 >= 3% 的股票数量
```

## 3. 板块评分归一化

系统会取前排板块的原始分，再归一化成 60-96 分：

```text
如果最高原始分 = 最低原始分：
  板块评分 = 82 - 排名序号 * 2

否则：
  板块评分 = 68 + (当前原始分 - 最低原始分) / (最高原始分 - 最低原始分) * 28

最终板块评分 = round(clamp(板块评分, 60, 96))
```

默认看板过滤：

```text
板块评分 >= 78
板块上涨广度 >= 45%
活跃股数量 >= 3
```

test 自动盘买入过滤更严格：

```text
板块评分 >= 84
```

## 4. 板块广度

```text
板块上涨广度 = 上涨家数 / max(上涨家数 + 下跌家数, 1)
```

默认要求：

```text
板块上涨广度 >= 45%
```

## 5. 可交易股票基础过滤

先过滤掉不可交易或不适合尾盘买入的股票：

```text
股票名称不能以 N 开头
股票名称不能包含 ST
股票名称不能包含 退市
```

默认候选过滤：

```text
当日涨幅 >= 3%
当日涨幅 <= 18.8%
换手率 >= 2%
换手率 <= 25%
量比 >= 1
```

如果“避开近涨停”开启：

```text
创业板/科创板：涨幅 >= 19% 过滤
北交所：涨幅 >= 29% 过滤
主板：涨幅 >= 9.6% 过滤
```

如果“仅创业板”开启：

```text
只有股票代码以 30 开头的创业板股票可以进入买入候选
科创板、北交所、主板股票只作为板块强度佐证
```

## 6. 前一交易日K线过滤

如果“前日K线”开启：

```text
前一交易日不能涨停
前一交易日不能跌停
前一交易日如果是阳线，阳线实体涨幅不能大于参数上限
```

默认参数：

```text
前日涨跌停过滤 = 开启
前日阳线实体上限 = 5%
```

公式：

```text
前日涨跌幅 = (前日收盘价 - 前前日收盘价) / 前前日收盘价 * 100

前日阳线实体涨幅 = (前日收盘价 - 前日开盘价) / 前日开盘价 * 100
```

涨跌停阈值：

```text
创业板/科创板：abs(前日涨跌幅) >= 19% 视为触及涨跌停过滤
北交所：abs(前日涨跌幅) >= 29% 视为触及涨跌停过滤
主板：abs(前日涨跌幅) >= 9.6% 视为触及涨跌停过滤
```

前日阳线过滤：

```text
如果 前日收盘价 > 前日开盘价
且 前日阳线实体涨幅 > maxPreviousBullBodyPct：
  过滤
```

日线数据不足时：

```text
前日K线过滤开启时，不放行
```

## 7. 日线多头

日线均线：

```text
MA5 = 最近5日收盘价平均值
MA10 = 最近10日收盘价平均值
MA20 = 最近20日收盘价平均值
```

日线多头定义：

```text
当前收盘价 >= MA5
且 MA5 > MA10
且 MA10 > MA20
```

默认看板：

```text
日线多头 = 关闭
```

test 自动盘：

```text
买入日线多头 = 开启
```

## 8. 个股状态评分

先根据当日涨幅和市场类型标记个股状态：

```text
20cm/30cm股票：
  涨幅 >= 29.5%：30cm近封
  涨幅 >= 19.5%：20cm确认
  涨幅 >= 15%：弹性前排
  涨幅 >= 8%：弹性承接

10cm股票：
  涨幅 >= 9.7%：10cm确认
  涨幅 >= 7%：10cm助攻

其他：
  涨幅 >= 5%：承接观察
  否则：观察
```

状态分：

```text
30cm近封：8
20cm确认：10
弹性前排：26
弹性承接：22
10cm确认：8
10cm助攻：14
其他：8
```

## 9. 个股评分公式

```text
个股评分 =
  板块评分 * 0.33
  + 状态分
  + 当日涨幅 * 0.55
  + 换手分
  + 涨幅位置分
  + min(量比, 5)
  + 日线分
  + 弹性偏好分

最终个股评分 = round(min(96, 个股评分))
```

换手分：

```text
如果 5% <= 换手率 <= 22%：
  换手分 = 10
如果 换手率 > 22%：
  换手分 = 5
否则：
  换手分 = 6
```

涨幅位置分：

```text
如果 5% <= 当日涨幅 <= 16%：
  涨幅位置分 = 8
如果 当日涨幅 > 18%：
  涨幅位置分 = 2
否则：
  涨幅位置分 = 4
```

日线分：

```text
如果日线多头：
  日线分 = 5
如果日线待确认：
  日线分 = 0
否则：
  日线分 = -2
```

弹性偏好分：

```text
如果“偏好20cm”开启，且股票属于20cm/30cm类型：
  弹性偏好分 = 3
否则：
  弹性偏好分 = 0
```

默认看板要求：

```text
个股评分 >= 76
```

test 自动盘要求：

```text
个股评分 >= 84
```

## 10. 风险标记

```text
如果换手率 >= 25%：
  风险 = 高换手
如果总市值 >= 300亿：
  风险 = 容量大
如果状态包含“近封”且 5% < 换手率 < 22%：
  风险 = 低
如果状态为观察类：
  风险 = 中
否则：
  风险 = 补涨
```

test 自动盘会过滤：

```text
风险不能是 高换手
```

## 11. 置信度

基础置信度：

```text
置信度 = clamp(个股评分, 45, 92)
```

风险扣分：

```text
如果风险 = 高换手：
  置信度 -= 8

如果风险 = 补涨 或 风险 = 中：
  置信度 -= 4
```

市场阶段扣分：

```text
如果 9:15-9:20 竞价可撤：
  置信度 -= 14

如果 9:20-9:30 竞价不可撤或开盘前：
  置信度 -= 8

如果休市/午间/复盘：
  置信度 -= 12
```

最终：

```text
置信度 = round(clamp(置信度, 35, 92))
```

test 自动盘要求：

```text
置信度 >= 70
```

## 12. 买入动作判定

```text
如果不是 14:30-14:57：
  不确认新买点

如果 14:30-14:57：
  如果 个股评分 >= 84 且 板块评分 >= 84 且 风险不是高换手：
    动作 = 尾盘候选
  否则如果 个股评分 >= 76：
    动作 = 等承接
  否则：
    动作 = 降级

如果 14:57-15:00：
  动作 = 不追
```

## 13. test 自动盘最终买入条件

test 自动盘只在尾盘窗口执行买入：

```text
14:30 <= 当前时间 < 14:57
```

最终买入必须同时满足：

```text
未持有该股票
当前价格 > 0
当日涨幅 >= 3%
当日涨幅 <= 18.8%
换手率 <= 25%
风险 != 高换手
板块评分 >= 84
个股评分 >= 84
置信度 >= 70
日线多头 = true
前日K线过滤通过
仅创业板过滤通过
未超过最大持仓数
如果每日一次买入开启，当日未执行过买入
```

排序规则：

```text
优先板块评分高
再看个股评分高
再看置信度高
```

## 14. 仓位公式

默认：

```text
最大持仓 = 3只
单票最大仓位 = 初始本金 * 30%
```

买入预算：

```text
单票预算 = min(初始本金 * 单票仓位比例, 当前现金 / 剩余仓位数)
```

买入股数：

```text
买入股数 = floor(单票预算 / 当前价格 / 100) * 100
```

如果买入股数小于 100：

```text
不买
```

## 15. 公式化总结

```text
强板块 = 板块评分达标 且 上涨广度达标 且 活跃股数量达标

可交易候选 =
  创业板股票
  且 非ST/非新股/非退市
  且 当日涨幅在参数区间
  且 换手率在参数区间
  且 量比达标
  且 非近涨停
  且 前日K线通过

尾盘候选 =
  可交易候选
  且 强板块
  且 个股评分达标
  且 14:30-14:57仍有强度

test买入 =
  尾盘候选
  且 板块评分 >= 84
  且 个股评分 >= 84
  且 置信度 >= 70
  且 日线多头
```
