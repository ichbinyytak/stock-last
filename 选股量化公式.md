# 尾盘买入法选股量化公式

记录版本：selection strategy v5

本文记录当前网站实际使用的选股量化规则。它是学习和模拟盘规则，不是投资建议。

## 1. 时间窗口

```text
如果 14:30 <= 当前时间 < 14:57：
  市场状态 = 尾盘窗口
  允许把合格股票标记为“尾盘候选”

如果 14:57 <= 当前时间 <= 15:00：
  市场状态 = 尾盘竞价
  只复盘，不追新仓

其他时间：
  只观察或复盘，不确认新买点
```

### 1.1 尾盘分钟涨幅排序

该规则默认关闭。开启后从14:30开始生效：

```text
N分钟涨幅 =
  (最新分钟收盘价 - N分钟前分钟收盘价)
  / N分钟前分钟收盘价 * 100

如果 N分钟涨幅 < 用户设置下限：
  过滤

剩余候选按N分钟涨幅降序排列
```

默认参数：

```text
分钟数 = 15
最低分钟涨幅 = 3%
```

14:30以前不执行此项过滤和排序。

## 2. 板块原始评分

对每个候选板块，先取板块成分股，再计算原始分：

```text
板块原始分 =
  max(板块涨幅, 0) * 5
  + max(板块换手率, 0) * 0.35
  + 板块强弱差 * 20
  + 20cm/30cm前排数量 * 8
  + 10cm助攻数量 * 5
  + 活跃股数量 * 0.7
  + 尾盘活跃候选数量 * 2.5
```

其中：

```text
板块强弱差 = (上涨家数 - 下跌家数) / max(上涨家数 + 下跌家数, 1)

20cm/30cm前排数量 =
  板块内创业板、科创板、北交所等20cm/30cm股票中，涨幅 >= 15% 的数量

10cm助攻数量 =
  板块内主板10cm股票中，涨幅 >= 9% 的数量

活跃股数量 =
  板块内涨幅 >= 5% 的股票数量

尾盘活跃候选数量 =
  板块内涨幅 >= 5% 且涨幅 <= 18% 且换手率 >= 3% 的股票数量
```

## 3. 板块评分归一化

系统会取前排板块的原始分，再归一化成 60-96 分：

```text
如果最高原始分 = 最低原始分：
  板块评分 = 82 - 排名序号 * 2

否则：
  板块评分 = 68 + (当前原始分 - 最低原始分) / (最高原始分 - 最低原始分) * 28

最终板块评分 = round(clamp(板块评分, 60, 96))
```

默认看板过滤：

```text
如果板块评分筛选开启：板块评分 >= 78
如果板块评分筛选关闭：不按板块过滤，只把板块评分作为展示和排序参考
```

test 自动盘买入过滤更严格：

```text
板块评分 >= 84
```

## 4. 板块广度

```text
板块上涨广度 = 上涨家数 / max(上涨家数 + 下跌家数, 1)
```

当前用途：

```text
板块上涨广度只参与板块评分和看板解释，不再作为用户单独配置的硬过滤条件
```

## 5. 可交易股票基础过滤

先过滤掉不可交易或不适合尾盘买入的股票：

```text
股票名称不能以 N 开头
股票名称不能包含 ST
股票名称不能包含 退市
```

默认候选过滤：

```text
涨幅筛选开启时：
  当日涨幅 >= 3%
  当日涨幅 <= 18.8%

涨幅筛选关闭时：
  当日涨幅只做展示，不参与候选过滤

涨幅区间建议：
  默认 3% - 18.8%
  偏宽松 1% - 18.8%
  偏严格 5% - 16%
换手筛选开启时：
  换手率 >= 2%
  换手率 <= 25%

换手筛选关闭时：
  换手率不参与候选过滤、个股评分和高换手风险判断

量比筛选开启时：
  量比 >= 1.0
  量比 <= 5.0

量比筛选关闭时：
  量比只做展示，不参与候选过滤

量比公式：
  量比 = 当前平均每分钟成交量 / 过去5个交易日平均每分钟成交量
```

流通市值筛选默认开启：

```text
流通市值 >= 0亿元
流通市值 <= 800亿元

流通市值筛选关闭时：
  流通市值只做展示，不参与候选过滤
```

说明：公开行情接口使用“流通市值”作为自由流通市值的近似口径。

如果“避开近涨停”开启：

```text
创业板/科创板：涨幅 >= 19% 过滤
北交所：涨幅 >= 29% 过滤
主板：涨幅 >= 9.6% 过滤
```

选股看板的展示市场由“选股策略参数”的“选股市场”多选项决定。实际自动买入市场由“操作策略参数”的“买入市场”多选项决定。

选股市场默认值：

```text
全部
```

可多选：

```text
创业板、科创板、主板、北交所

如果勾选“全部”：
  看板展示所有市场的合格候选
```

## 6. 前一交易日K线过滤

如果“前日涨跌停”开启：

```text
前一交易日不能涨停
前一交易日不能跌停
```

默认参数：

```text
前日涨跌停过滤 = 开启
```

公式：

```text
前日涨跌幅 = (前日收盘价 - 前前日收盘价) / 前前日收盘价 * 100

```

涨跌停阈值：

```text
创业板/科创板：abs(前日涨跌幅) >= 19% 视为触及涨跌停过滤
北交所：abs(前日涨跌幅) >= 29% 视为触及涨跌停过滤
主板：abs(前日涨跌幅) >= 9.6% 视为触及涨跌停过滤
```


昨日最高涨幅默认开启：

```text
昨日实际涨幅 = (昨日收盘价 - 前日收盘价) / 前日收盘价 * 100

如果 昨日实际涨幅 >= 5%：
  过滤
```

日线数据不足时：

```text
前日涨跌停过滤开启时，不放行
```

## 7. 日线多头

日线均线：

```text
MA5 = 最近5日收盘价平均值
MA10 = 最近10日收盘价平均值
MA20 = 最近20日收盘价平均值
```

日线多头定义：

```text
当前收盘价 >= MA5
且 MA5 > MA10
且 MA10 > MA20
```

默认看板：

```text
日线多头 = 关闭
```

test 自动盘：

```text
买入日线多头 = 开启
```

## 8. 中短期涨幅与成交量过滤

周期累计涨幅限制默认开启：

```text
N日累计涨幅 = (最新收盘价 - N个交易日前收盘价) / N个交易日前收盘价 * 100

默认：N = 10
默认：N日累计涨幅 >= -100%
默认：N日累计涨幅 <= 60%

如果 N日累计涨幅 < 最低区间涨幅：
  过滤
如果 N日累计涨幅 > 最高区间涨幅：
  过滤
```

成交量限制默认开启：

```text
120日平均成交量 = 最近120个交易日成交量平均值

10日内放量天数 =
  最近10个交易日中，成交量 > 120日平均成交量的交易日数量

默认：10日内至少3天成交量大于120日平均成交量

如果 10日内放量天数 < 3：
  过滤
```

新高观察周期默认开启：

```text
N日高点 =
  最近N个已完成交易日的最高价最大值

默认：N = 60

如果 当前实时价 < N日高点：
  过滤
```

日线数据不足以完成已开启的过滤规则时，不放行。

## 9. 个股状态评分

先根据当日涨幅和市场类型标记个股状态：

```text
20cm/30cm股票：
  涨幅 >= 29.5%：30cm近封
  涨幅 >= 19.5%：20cm确认
  涨幅 >= 15%：弹性前排
  涨幅 >= 8%：弹性承接

10cm股票：
  涨幅 >= 9.7%：10cm确认
  涨幅 >= 7%：10cm助攻

其他：
  涨幅 >= 5%：承接观察
  否则：观察
```

状态分：

```text
30cm近封：8
20cm确认：10
弹性前排：26
弹性承接：22
10cm确认：8
10cm助攻：14
其他：8
```

## 10. 个股评分公式

个股评分不是原始行情指标，而是系统根据前面的量化条件自动计算出来的综合展示分。

用户调整涨幅区间、换手区间、量比区间、日线多头、新高观察周期、周期涨幅、流通市值、板块评分等参数后，个股评分会自动变化。

```text
个股评分 =
  板块评分 * 0.33
  + 状态分
  + 当日涨幅 * 0.55
  + 换手分
  + 涨幅位置分
  + 量比分
  + 日线分

最终个股评分 = round(min(96, 个股评分))
```

影响方向：

```text
板块评分越高：个股基础分越高
状态越强：弹性前排、弹性承接、10cm助攻等状态加分越高
当日涨幅越高：涨幅贡献越高，但超过极端区间后涨幅位置分会降低
换手筛选开启且换手处于合理区间：换手分更高
量比筛选开启且量比合理放大：量比分更高
日线多头：加分
未多头：扣分
20cm弹性通过默认创业板、选股市场、个股状态和板块前排自然体现，不再单独设置偏好加分
```

使用建议：

```text
个股评分适合作为综合排序和展示参考
不建议只用个股评分单独决定买入
如果已经启用了清晰的量化条件，个股评分可以理解为这些条件共同作用后的派生分
看板选股不提供个股评分输入框，也不把个股评分作为硬筛选
```

选股页策略评分：

```text
策略评分 =
  基础50
  + 涨幅筛选严格度
  + 换手筛选严格度
  + 量比筛选严格度
  + 板块评分筛选严格度
  + 市场范围严格度
  + 日线多头、10日涨幅、成交量限制、新高观察周期、前日涨跌停、流通市值等可选开关加减分

分数越高：筛选越严格，策略越保守，候选越少
分数越低：筛选越宽松，策略越激进，候选越多

78-100：保守
62-77：偏保守
45-61：平衡
30-44：偏激进
0-29：激进
```

换手分：

```text
如果“换手筛选”关闭：
  换手分 = 8（固定中性分，不根据换手率变化）

如果“换手筛选”开启：
如果 5% <= 换手率 <= 22%：
  换手分 = 10
如果 换手率 > 22%：
  换手分 = 5
否则：
  换手分 = 6

量比分：

```text
如果“量比筛选”关闭：
  量比分 = 2（固定中性分，不根据量比变化）

如果“量比筛选”开启：
  量比分 = min(量比, 5)
```
```

涨幅位置分：

```text
如果 5% <= 当日涨幅 <= 16%：
  涨幅位置分 = 8
如果 当日涨幅 > 18%：
  涨幅位置分 = 2
否则：
  涨幅位置分 = 4
```

日线分：

```text
如果日线多头：
  日线分 = 5
如果日线待确认：
  日线分 = 0
否则：
  日线分 = -2
```

默认看板要求：

```text
个股评分只展示和排序，不作为硬筛选
```

test 自动盘要求：

```text
自动模拟买入仍要求 个股评分 >= 84
```

## 11. 风险标记

```text
如果换手率 >= 25%：
  风险 = 高换手
如果总市值 >= 300亿：
  风险 = 容量大
如果状态包含“近封”且 5% < 换手率 < 22%：
  风险 = 低
如果状态为观察类：
  风险 = 中
否则：
  风险 = 补涨
```

test 自动盘会过滤：

```text
风险不能是 高换手
```

## 12. 置信度

基础置信度：

```text
置信度 = clamp(个股评分, 45, 92)
```

风险扣分：

```text
如果风险 = 高换手：
  置信度 -= 8

如果风险 = 补涨 或 风险 = 中：
  置信度 -= 4
```

市场阶段扣分：

```text
如果 9:15-9:20 竞价可撤：
  置信度 -= 14

如果 9:20-9:30 竞价不可撤或开盘前：
  置信度 -= 8

如果休市/午间/复盘：
  置信度 -= 12
```

最终：

```text
置信度 = round(clamp(置信度, 35, 92))
```

test 自动盘要求：

```text
置信度 >= 70
```

## 13. 买入动作判定

```text
如果不是 14:30-14:57：
  不确认新买点

如果 14:30-14:57：
  如果 个股评分 >= 84 且 板块评分 >= 84 且 风险不是高换手：
    动作 = 尾盘候选
  否则如果 个股评分 >= 76：
    动作 = 等承接
  否则：
    动作 = 降级

如果 14:57-15:00：
  动作 = 不追
```

## 14. test 自动盘最终买入条件

test 自动盘只在尾盘窗口执行买入：

```text
14:30 <= 当前时间 < 14:57
```

最终买入必须同时满足：

```text
未持有该股票
当前价格 > 0
涨幅筛选通过
换手率 <= 25%
风险 != 高换手
板块评分 >= 84
个股评分 >= 84
置信度 >= 70
日线多头 = true
前日涨跌停过滤通过
昨日实际涨幅过滤通过
周期累计涨幅过滤通过
成交量限制过滤通过
新高观察周期过滤通过
流通市值过滤通过
买入市场过滤通过
未超过最大持仓数
如果每日一次买入开启，当日未执行过买入
```

排序规则：

```text
优先板块评分高
再看个股评分高
再看置信度高
```

## 15. 仓位公式

默认：

```text
最大持仓 = 3只
单票最大仓位 = 初始本金 * 30%
```

买入预算：

```text
单票预算 = min(初始本金 * 单票仓位比例, 当前现金 / 剩余仓位数)
```

买入股数：

```text
买入股数 = floor(单票预算 / 当前价格 / 100) * 100
```

如果买入股数小于 100：

```text
不买
```

## 16. 公式化总结

```text
强板块 = 板块评分筛选关闭 或 板块评分达标

可交易候选 =
  创业板股票
  且 非ST/非新股/非退市
  且 股票所属市场在选股市场范围内
  且 涨幅筛选关闭 或 当日涨幅在参数区间
  且 换手筛选关闭 或 换手率在参数区间
  且 量比筛选关闭 或 量比在参数区间
  且 非近涨停
  且 前日涨跌停通过

尾盘候选 =
  可交易候选
  且 强板块
  且 个股评分用于排序和动作分级
  且 14:30-14:57仍有强度

test买入 =
  尾盘候选
  且 板块评分 >= 84
  且 个股评分 >= 84
  且 置信度 >= 70
  且 日线多头
  且 股票所属市场在买入市场范围内
```

买入市场默认值：

```text
创业板
```

可多选：

```text
创业板、科创板、主板、北交所

如果勾选“全部”：
  允许操作所有市场
```
